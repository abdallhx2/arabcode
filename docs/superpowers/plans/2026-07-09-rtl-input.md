# RTL Input (Phase 2 — Mirror Display Buffer) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Arabic typed into the TUI prompt/textarea renders shaped, reordered (RTL), and right-aligned while typing, with the cursor tracking the visual insertion point and arrow keys moving visually — without touching the native Zig edit buffer's logical editing behavior.

**Architecture:** The Zig `EditBuffer` stays the logical source of truth (insert/delete/undo/wrap/height-measure unchanged). We intercept **drawing only**: a mirror `TextBuffer` holds the shaped+reordered visual form of each native-wrapped virtual line and is drawn instead of `drawEditorView` when the content contains RTL. The JS-side `renderCursor` remaps the logical cursor column through a per-line insertion-point map. Arrow left/right handlers swap when the current line is RTL-base. All wiring goes through the existing `globalThis.__arabcodeRtl` hook object (new `editor` sub-hook) installed by `packages/tui/src/util/rtl-hook.ts`, with the `@opentui/core` side maintained via `bun patch`.

**Tech Stack:** Bun + TypeScript, SolidJS via `@opentui/solid`, `@opentui/core@0.4.3` (patched via `patches/@opentui%2Fcore@0.4.3.patch`), `bidi-js` (UAX#9), existing `packages/tui/src/util/rtl.ts` pipeline (shaping/reorder), `bun:test` + `testRender`/`captureCharFrame` for frame tests.

**Spec:** `docs/superpowers/specs/2026-07-09-rtl-input-design.md`

## Global Constraints

- Mirror activates only when `rtlMode() === "app"` AND the editor content matches `hasRtl()` — pure-LTR content must go through the original native path byte-for-byte (zero cost).
- Logical columns are counted on the **raw** (unshaped) text — that is how Zig counts them. Visual columns are counted on the **shaped** text (lam-alef merges 2 logical cols → 1 visual col).
- Native wrap boundaries are reused via `editorView.getLineInfo()` so virtual-line count always matches native height measurement — never re-wrap independently.
- All comments/docs in new code are Arabic, matching the existing `rtl.ts`/`rtl-hook.ts` style.
- Node modules edits must be persisted with `bun patch --commit 'node_modules/@opentui/core'` — never leave node_modules edited without regenerating the patch file.
- Insertion-point convention (mirror of LTR block-cursor): in an RTL run the terminal cursor sits on the empty cell **left** of the last typed character, i.e. `visualStart(precedingCell) - 1`; at line start of an RTL line it is `totalVisualWidth - 1`. Raw map values may be `-1`; the consumer clamps with `Math.max(0, value + pad)`.
- Commit messages end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Pre-existing on branch: ~36 failing TUI tests from the translation phase (English-string assertions) plus `app-lifecycle.test.tsx` epilogue test — NOT caused by this work; only ensure the rtl test files pass and no NEW failures appear elsewhere.

---

### Task 1: `visualLineMap` — single-line visual transform with cursor insertion map

**Files:**
- Modify: `packages/tui/src/util/rtl.ts` (append after `visualLines`, around line 344)
- Test: `packages/tui/test/util/rtl-map.test.ts` (create)

**Interfaces:**
- Consumes (already in `rtl.ts`, module-internal access): `toCells(text, ref?)`, `shapeCells(cells)`, `reorderCells(cells)`, `cellWidth(cell)` (private — the new function lives in the same file), `hasRtl(text)`, `bidi` instance.
- Produces (used by Task 2):
  ```ts
  export interface VisualLineMap {
    cells: RtlCell[]        // الترتيب البصري، مشكّلة
    cursor: number[]        // الفهرس = عمود منطقي 0..العرض المنطقي؛ القيمة = عمود بصري (قد تكون -1)
    rtlBase: boolean        // قاعدة السطر RTL (مستوى الفقرة فردي)
  }
  export function visualLineMap(text: string): VisualLineMap
  ```

- [ ] **Step 1: Write the failing tests**

Create `packages/tui/test/util/rtl-map.test.ts`:

```ts
import { describe, expect, test } from "bun:test"
import { visualLineMap } from "../../src/util/rtl"

describe("visualLineMap", () => {
  test("سطر لاتيني: خريطة هوية بلا قاعدة RTL", () => {
    const m = visualLineMap("abc")
    expect(m.rtlBase).toBe(false)
    expect(m.cursor).toEqual([0, 1, 2, 3])
    expect(m.cells.map((c) => c.text).join("")).toBe("abc")
  })

  test("سطر فارغ", () => {
    const m = visualLineMap("")
    expect(m.cursor).toEqual([0])
    expect(m.rtlBase).toBe(false)
  })

  test("عربي صافٍ: المؤشر يتحرك يساراً مع الكتابة", () => {
    // "ابج" خام: ا=0 ب=1 ج=2 (3 أعمدة). بصري: [ج ب ا] (ا في أقصى اليمين).
    // نقطة الإدراج = يسار الحرف السابق: بداية السطر → 2، بعد ا → 1، بعد ب → 0، بعد ج → -1
    const m = visualLineMap("ابج")
    expect(m.rtlBase).toBe(true)
    expect(m.cursor).toEqual([2, 1, 0, -1])
  })

  test("مختلط عربي/لاتيني بقاعدة RTL", () => {
    // "اب cd" منطقي: ا0 ب1 ␣2 c3 d4 — بصري: "cd با" → c0 d1 ␣2 ب3 ا4
    const m = visualLineMap("اب cd")
    expect(m.rtlBase).toBe(true)
    expect(m.cursor[0]).toBe(4) // بداية السطر: يمين الكل
    expect(m.cursor[1]).toBe(3) // بعد ا (مستوى RTL): يسار ا
    expect(m.cursor[2]).toBe(2) // بعد ب
    expect(m.cursor[4]).toBe(1) // بعد c (مستوى LTR زوجي): يمين c
    expect(m.cursor[5]).toBe(2) // بعد d: يمين d
  })

  test("لام-ألف: عمودان منطقيان → خلية بصرية واحدة، والفجوة تُملأ", () => {
    // "لا" خام عموداً: ل=0 ا=1 (عرض 2). مشكّل: خلية واحدة (عرض بصري 1).
    const m = visualLineMap("لا")
    expect(m.rtlBase).toBe(true)
    expect(m.cursor[0]).toBe(0) // العرض البصري 1 → البداية عند 0
    expect(m.cursor[2]).toBe(-1) // بعد الألف: يسار الرباط
    expect(m.cursor[1]).toBe(m.cursor[2]) // داخل الرباط: تُملأ من الحد التالي
  })

  test("القيم البصرية للخلايا مطابقة لـ visualLine", () => {
    const m = visualLineMap("سلام")
    expect(m.cells.map((c) => c.text).join("")).toContain("ﻡ") // م منعزلة في أقصى البصري
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/rtl-map.test.ts`
Expected: FAIL — `visualLineMap` is not exported.

- [ ] **Step 3: Implement `visualLineMap` in `rtl.ts`**

Append after the existing `visualLines` function (before `ChunkLike`):

```ts
// ---------------------------------------------------------------------------
// خريطة المؤشر (المرحلة 2: الإدخال)
// ---------------------------------------------------------------------------

export interface VisualLineMap {
  /** الخلايا بالترتيب البصري (مشكّلة) */
  cells: RtlCell[]
  /** نقطة الإدراج: الفهرس = عمود منطقي (0..العرض المنطقي)، القيمة = عمود بصري (قد تكون -1) */
  cursor: number[]
  /** قاعدة السطر RTL (مستوى الفقرة فردي — أول حرف قوي) */
  rtlBase: boolean
}

/**
 * تحويل سطر واحد إلى شكله البصري مع خريطة مواضع الإدراج للمؤشر.
 * الأعمدة المنطقية تُحسب على النص الخام (كما يعدّها محرر Zig — قبل التشكيل)،
 * والبصرية على النص المشكّل. نقطة الإدراج في مقطع RTL تجلس على الخلية
 * الفارغة يسار الحرف السابق (مرآة عرف المؤشر في LTR)، وتتبع مستوى الحرف
 * السابق عند الحدود المختلطة (UAX#9). القيم قد تكون -1 (السطر ممتلئ) —
 * القصّ على المستهلك.
 */
export function visualLineMap(text: string): VisualLineMap {
  const raw = toCells(text)
  const rawCols: number[] = []
  let logicalWidth = 0
  for (const cell of raw) {
    rawCols.push(logicalWidth)
    logicalWidth += cellWidth(cell)
  }
  rawCols.push(logicalWidth)
  if (!hasRtl(text)) {
    return { cells: raw, cursor: Array.from({ length: logicalWidth + 1 }, (_, i) => i), rtlBase: false }
  }
  // كل خلية خام تُعلَّم بفهرسها لتتبعها عبر دمج التشكيل وإعادة الترتيب
  const shaped = shapeCells(raw.map((cell, i) => ({ text: cell.text, ref: i })))
  const shapedStarts: number[] = []
  let shapedText = ""
  for (const cell of shaped) {
    shapedStarts.push(shapedText.length)
    shapedText += cell.text
  }
  const levels = bidi.getEmbeddingLevels(shapedText)
  const rtlBase = (levels.paragraphs[0]?.level ?? 0) % 2 === 1
  const visual = reorderCells(shaped)
  // العمود البصري لكل خلية مشكّلة، متعقبة بمرجعها (فهرس أول خلية خام فيها)
  const visualStart = new Map<number, number>()
  const visualEnd = new Map<number, number>()
  let vcol = 0
  for (const cell of visual) {
    visualStart.set(cell.ref as number, vcol)
    vcol += cellWidth(cell)
    visualEnd.set(cell.ref as number, vcol)
  }
  const cursor = new Array<number>(logicalWidth + 1).fill(NaN)
  cursor[0] = rtlBase ? vcol - 1 : 0
  for (let j = 0; j < shaped.length; j++) {
    const firstRaw = shaped[j]!.ref as number
    // نهاية النطاق الخام لهذه الخلية = بداية الخلية المشكّلة التالية (الدمج متجاور فقط)
    const nextRaw = j + 1 < shaped.length ? (shaped[j + 1]!.ref as number) : raw.length
    const level = levels.levels[shapedStarts[j]!]!
    cursor[rawCols[nextRaw]!] = level % 2 === 1 ? visualStart.get(firstRaw)! - 1 : visualEnd.get(firstRaw)!
  }
  // ملء الفجوات (داخل لام-ألف/الحروف العريضة) بقيمة أول حد تالٍ معرّف
  for (let i = logicalWidth - 1; i > 0; i--) {
    if (Number.isNaN(cursor[i])) cursor[i] = cursor[i + 1]!
  }
  return { cells: visual, cursor, rtlBase }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/rtl-map.test.ts`
Expected: PASS (6 tests). If the mixed-text expectations mismatch, print `visualLineMap("اب cd")` and verify against `bidi-js` ground truth (`bidi.getReorderedString("اب cd", bidi.getEmbeddingLevels("اب cd"))`) before adjusting expectations — the implementation rule (preceding-char level) is the invariant, not the literal numbers.

Also run the existing suite to catch regressions: `bun test test/util/rtl.test.ts` → all 33 still PASS.

- [ ] **Step 5: Typecheck and commit**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun run typecheck` (or `bunx tsgo --noEmit` if no script)
Expected: clean.

```bash
git add packages/tui/src/util/rtl.ts packages/tui/test/util/rtl-map.test.ts
git commit -m "feat(tui): visualLineMap — cursor insertion-point map for RTL input

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: `rtl-editor.ts` — mirror state, virtual-line slicing, draw/cursor/arrows hooks

**Files:**
- Create: `packages/tui/src/util/rtl-editor.ts`
- Test: `packages/tui/test/util/rtl-editor.test.ts` (create — unit tests for `sliceVirtualLines` only; `draw`/`cursorCol` are covered by Task 4 integration tests)

**Interfaces:**
- Consumes: `visualLineMap(text)` from Task 1, `hasRtl`, `cellsWidth` from `rtl.ts`; `TextBuffer.create(widthMethod)`, `TextBufferView.create(buffer)`, `view.setWrapMode("none")`, `view.setViewport(x,y,w,h)`, `buffer.setStyledText(new StyledText(chunks))` from `@opentui/core` (verified exported).
- Produces (registered by Task 3 as `globalThis.__arabcodeRtl.editor`):
  ```ts
  export const editorHooks: {
    draw(renderable: unknown, frameBuffer: unknown): boolean
    cursorCol(renderable: unknown, cursor: { visualRow: number; visualCol: number }): number | undefined
    swapArrows(renderable: unknown): boolean
    dispose(renderable: unknown): void
  }
  export function sliceVirtualLines(
    text: string,
    info: { lineWidthCols: number[]; lineSources: number[] },
  ): string[]
  ```

- [ ] **Step 1: Write the failing tests for `sliceVirtualLines`**

Create `packages/tui/test/util/rtl-editor.test.ts`:

```ts
import { describe, expect, test } from "bun:test"
import { sliceVirtualLines } from "../../src/util/rtl-editor"

describe("sliceVirtualLines", () => {
  test("أسطر منطقية بلا التفاف", () => {
    expect(sliceVirtualLines("ab cd\nef", { lineWidthCols: [5, 2], lineSources: [0, 1] })).toEqual(["ab cd", "ef"])
  })

  test("التفاف يأكل مسافة الفاصل", () => {
    // "ab cd" بعرض 2: Zig يلتف إلى ["ab","cd"] — المسافة مأكولة عند الحد
    expect(sliceVirtualLines("ab cd", { lineWidthCols: [2, 2], lineSources: [0, 0] })).toEqual(["ab", "cd"])
  })

  test("التفاف بمسافة محسوبة ضمن عرض السطر", () => {
    // لو أبقى Zig المسافة داخل السطر الأول (عرض 3) فالمشي بالعرض يستهلكها
    expect(sliceVirtualLines("ab cd", { lineWidthCols: [3, 2], lineSources: [0, 0] })).toEqual(["ab ", "cd"])
  })

  test("سطر فارغ بين سطرين", () => {
    expect(sliceVirtualLines("ا\n\nب", { lineWidthCols: [1, 0, 1], lineSources: [0, 1, 2] })).toEqual(["ا", "", "ب"])
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/rtl-editor.test.ts`
Expected: FAIL — module `../../src/util/rtl-editor` not found.

- [ ] **Step 3: Implement `rtl-editor.ts`**

Create `packages/tui/src/util/rtl-editor.ts` with this complete content:

```ts
/**
 * مرآة العرض لمحرر الإدخال (المرحلة 2 من RTL).
 *
 * مخزن Zig يبقى مصدر الحقيقة المنطقي (كتابة/حذف/تراجع/التفاف/قياس ارتفاع)،
 * وهذه الوحدة تعترض الرسم فقط: عند وجود نص RTL تُبنى نسخة بصرية (تشكيل +
 * إعادة ترتيب لكل سطر افتراضي بنفس نقاط التفاف Zig من getLineInfo) في
 * TextBuffer مرآتي يُرسم بدل drawEditorView، مع خريطة تعيد عمود المؤشر
 * المنطقي إلى موضعه البصري، وقلب أسهم يمين/يسار في الأسطر عربية القاعدة.
 */
import { StyledText, TextBuffer, TextBufferView, type TextChunk } from "@opentui/core"
import { cellsWidth, hasRtl, visualLineMap } from "./rtl"

/** ما يحتاجه الترقيع من EditBufferRenderable (بنية داخلية في @opentui/core) */
interface EditorLike {
  width: number
  height: number
  _screenX: number
  _screenY: number
  _textColor: unknown
  _ctx: { widthMethod: "wcwidth" | "unicode" }
  editBuffer: { getText(): string }
  editorView: {
    getViewport(): { offsetY: number }
    getLineInfo(): { lineWidthCols: number[]; lineSources: number[] }
    getVisualCursor(): { visualRow: number; visualCol: number }
  }
}

interface MirrorLine {
  /** عمود منطقي (داخل السطر الافتراضي) → عمود بصري نهائي (بعد المحاذاة) */
  cursor: number[]
  rtlBase: boolean
}

interface Mirror {
  buffer: TextBuffer
  view: TextBufferView
  /** بصمة آخر بناء: النص + العرض + إزاحة المنفذ */
  fingerprint: string
  lines: MirrorLine[]
}

const mirrors = new WeakMap<object, Mirror>()

const segmenter = new Intl.Segmenter(undefined, { granularity: "grapheme" })

/**
 * تقطيع النص المنطقي إلى أسطر افتراضية بنفس حدود التفاف Zig
 * (لا نلتف بأنفسنا — عدد الأسطر يطابق قياس الارتفاع الأصلي بالبناء).
 * مسافة الالتفاف المأكولة تُتخطى عند سطر تالٍ من نفس المصدر المنطقي.
 */
export function sliceVirtualLines(
  text: string,
  info: { lineWidthCols: number[]; lineSources: number[] },
): string[] {
  const logical = text.split("\n")
  const out: string[] = []
  let source = -1
  let rest: string[] = []
  let restIdx = 0
  for (let v = 0; v < info.lineWidthCols.length; v++) {
    if (info.lineSources[v] !== source) {
      source = info.lineSources[v]!
      rest = [...segmenter.segment(logical[source] ?? "")].map((s) => s.segment)
      restIdx = 0
    } else if (rest[restIdx] === " ") {
      restIdx++
    }
    let width = 0
    let line = ""
    while (restIdx < rest.length && width < info.lineWidthCols[v]!) {
      width += Bun.stringWidth(rest[restIdx]!)
      line += rest[restIdx]!
      restIdx++
    }
    out.push(line)
  }
  return out
}

function rebuild(r: EditorLike, mirror: Mirror, text: string): void {
  const offsetY = r.editorView.getViewport().offsetY
  const height = Math.max(1, r.height)
  const linesText = sliceVirtualLines(text, r.editorView.getLineInfo()).slice(offsetY, offsetY + height)
  const lines: MirrorLine[] = []
  const rows: string[] = []
  for (const lineText of linesText) {
    const m = visualLineMap(lineText)
    const pad = m.rtlBase ? Math.max(0, r.width - cellsWidth(m.cells)) : 0
    rows.push(" ".repeat(pad) + m.cells.map((c) => c.text).join(""))
    lines.push({ cursor: m.cursor.map((c) => Math.max(0, c + pad)), rtlBase: m.rtlBase })
  }
  mirror.lines = lines
  const chunk = { __isChunk: true, text: rows.join("\n"), fg: r._textColor } as unknown as TextChunk
  mirror.buffer.setStyledText(new StyledText([chunk]))
  mirror.view.setViewport(0, 0, Math.max(1, r.width), height)
}

/** يرسم المرآة بدل drawEditorView. يعيد false (المسار الأصلي) لنص بلا RTL. */
function draw(renderable: unknown, frameBuffer: unknown): boolean {
  const r = renderable as EditorLike
  const text = r.editBuffer.getText()
  if (!hasRtl(text)) {
    const stale = mirrors.get(renderable as object)
    if (stale) {
      // محتوى سابق كان عربياً وتبدّل: تعطيل خريطة المؤشر حتى عودة RTL
      stale.lines = []
      stale.fingerprint = ""
    }
    return false
  }
  let mirror = mirrors.get(renderable as object)
  if (!mirror) {
    const buffer = TextBuffer.create(r._ctx.widthMethod)
    const view = TextBufferView.create(buffer)
    view.setWrapMode("none")
    mirror = { buffer, view, fingerprint: "", lines: [] }
    mirrors.set(renderable as object, mirror)
  }
  const fingerprint = `${r.editorView.getViewport().offsetY} ${r.width} ${text}`
  if (fingerprint !== mirror.fingerprint) {
    rebuild(r, mirror, text)
    mirror.fingerprint = fingerprint
  }
  const fb = frameBuffer as { drawTextBuffer(view: TextBufferView, x: number, y: number): void }
  fb.drawTextBuffer(mirror.view, r._screenX, r._screenY)
  return true
}

/** عمود المؤشر البصري المعاد تحويله؛ undefined = استخدم العمود الأصلي. */
function cursorCol(renderable: unknown, cursor: { visualRow: number; visualCol: number }): number | undefined {
  return mirrors.get(renderable as object)?.lines[cursor.visualRow]?.cursor[cursor.visualCol]
}

/** هل يُقلب سهما يمين/يسار (تحرك بصري) — سطر المؤشر الحالي عربي القاعدة. */
function swapArrows(renderable: unknown): boolean {
  const mirror = mirrors.get(renderable as object)
  if (!mirror || mirror.lines.length === 0) return false
  const r = renderable as EditorLike
  return mirror.lines[r.editorView.getVisualCursor().visualRow]?.rtlBase ?? false
}

/** تحرير الموارد الأصلية للمرآة عند تدمير المكوّن. */
function dispose(renderable: unknown): void {
  const mirror = mirrors.get(renderable as object)
  if (!mirror) return
  mirror.view.destroy()
  mirror.buffer.destroy()
  mirrors.delete(renderable as object)
}

export const editorHooks = { draw, cursorCol, swapArrows, dispose }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/rtl-editor.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Typecheck and commit**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun run typecheck`
Expected: clean. (If `TextBuffer`/`TextBufferView` are missing from the package's type surface, import them with `// @ts-expect-error` — they ARE runtime exports, verified — and note it in the commit body.)

```bash
git add packages/tui/src/util/rtl-editor.ts packages/tui/test/util/rtl-editor.test.ts
git commit -m "feat(tui): RTL editor mirror — native-wrap slicing + mirror buffer + cursor/arrow hooks

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Wire the hooks — patch `@opentui/core` and register `editor` in `rtl-hook.ts`

**Files:**
- Modify: `packages/tui/src/util/rtl-hook.ts` (global type declaration + `installRtlHooks`)
- Modify (patched dep): `node_modules/@opentui/core/index-xt9f071j.js` — `EditBufferRenderable.renderSelf` (~line 6138), `renderCursor` (~6141), `destroy` (~6167)
- Modify (patched dep): `node_modules/@opentui/core/index.js` — `TextareaRenderable.buildActionHandlers` (~line 5148)
- Regenerate: `patches/@opentui%2Fcore@0.4.3.patch`

**Interfaces:**
- Consumes: `editorHooks` from Task 2.
- Produces: `globalThis.__arabcodeRtl.editor` with the four functions; patched core calls them optionally (`?.`) so unpatched/off mode is unaffected.

- [ ] **Step 1: Extend `rtl-hook.ts`**

In `packages/tui/src/util/rtl-hook.ts`:

Add import at top (after the existing imports):

```ts
import { editorHooks } from "./rtl-editor"
```

In the `declare global` block, extend the `__arabcodeRtl` object type with:

```ts
        /** خطافات محرر الإدخال (المرحلة 2): مرآة العرض + خريطة المؤشر + قلب الأسهم */
        editor: {
          draw(renderable: unknown, frameBuffer: unknown): boolean
          cursorCol(renderable: unknown, cursor: { visualRow: number; visualCol: number }): number | undefined
          swapArrows(renderable: unknown): boolean
          dispose(renderable: unknown): void
        }
```

In `installRtlHooks()`, change the registration line to:

```ts
  globalThis.__arabcodeRtl = { transform, resize, line, chunks, editor: editorHooks }
```

- [ ] **Step 2: Prepare the dependency for patching**

```bash
cd /home/abotrf/Desktop/opencode && bun patch @opentui/core@0.4.3
```

Expected output mentions the package is ready to patch in `node_modules/@opentui/core` (existing patch content is preserved — verify the previous round's hooks are still present: `grep -n "__arabcodeRtl" node_modules/@opentui/core/index-xt9f071j.js` shows the transform/resize/drawBox hooks).

- [ ] **Step 3: Patch `EditBufferRenderable` in `node_modules/@opentui/core/index-xt9f071j.js`**

Replace (around line 6138):

```js
  renderSelf(buffer) {
    buffer.drawEditorView(this.editorView, this._screenX, this._screenY);
  }
  renderCursor(buffer) {
    if (!this._showCursor || !this._focused)
      return;
    const visualCursor = this.editorView.getVisualCursor();
    const screenX = this._screenX;
    const screenY = this._screenY;
    const cursorX = screenX + visualCursor.visualCol + 1;
```

with:

```js
  renderSelf(buffer) {
    if (globalThis.__arabcodeRtl?.editor?.draw(this, buffer)) return;
    buffer.drawEditorView(this.editorView, this._screenX, this._screenY);
  }
  renderCursor(buffer) {
    if (!this._showCursor || !this._focused)
      return;
    const visualCursor = this.editorView.getVisualCursor();
    const rtlCol = globalThis.__arabcodeRtl?.editor?.cursorCol(this, visualCursor);
    const screenX = this._screenX;
    const screenY = this._screenY;
    const cursorX = screenX + (rtlCol ?? visualCursor.visualCol) + 1;
```

And in `destroy()` (around line 6167), after the `if (this.isDestroyed) return;` guard, add:

```js
    globalThis.__arabcodeRtl?.editor?.dispose(this);
```

- [ ] **Step 4: Patch `TextareaRenderable.buildActionHandlers` in `node_modules/@opentui/core/index.js`**

Replace (around line 5148):

```js
      ["move-left", () => this.moveCursorLeft()],
      ["move-right", () => this.moveCursorRight()],
      ["move-up", () => this.moveCursorUp()],
      ["move-down", () => this.moveCursorDown()],
      ["select-left", () => this.moveCursorLeft({ select: true })],
      ["select-right", () => this.moveCursorRight({ select: true })],
```

with:

```js
      ["move-left", () => (globalThis.__arabcodeRtl?.editor?.swapArrows(this) ? this.moveCursorRight() : this.moveCursorLeft())],
      ["move-right", () => (globalThis.__arabcodeRtl?.editor?.swapArrows(this) ? this.moveCursorLeft() : this.moveCursorRight())],
      ["move-up", () => this.moveCursorUp()],
      ["move-down", () => this.moveCursorDown()],
      ["select-left", () => (globalThis.__arabcodeRtl?.editor?.swapArrows(this) ? this.moveCursorRight({ select: true }) : this.moveCursorLeft({ select: true }))],
      ["select-right", () => (globalThis.__arabcodeRtl?.editor?.swapArrows(this) ? this.moveCursorLeft({ select: true }) : this.moveCursorRight({ select: true }))],
```

- [ ] **Step 5: Persist the patch**

```bash
cd /home/abotrf/Desktop/opencode && bun patch --commit 'node_modules/@opentui/core'
```

Expected: `patches/@opentui%2Fcore@0.4.3.patch` regenerated (`git diff --stat patches/` shows growth). Verify with `grep -c "editor" 'patches/@opentui%2Fcore@0.4.3.patch'` > 0.

- [ ] **Step 6: Sanity-run existing RTL suites (hooks must not break display path)**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/rtl.test.ts test/util/rtl-render.test.tsx test/util/rtl-map.test.ts test/util/rtl-editor.test.ts`
Expected: all PASS. Then `bun run typecheck` → clean.

- [ ] **Step 7: Commit**

```bash
git add packages/tui/src/util/rtl-hook.ts 'patches/@opentui%2Fcore@0.4.3.patch' package.json
git commit -m "feat(tui): wire RTL editor mirror into EditBufferRenderable render/cursor/arrow paths

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Integration tests — typed Arabic in a real textarea frame

**Files:**
- Modify: `packages/tui/test/util/rtl-render.test.tsx` (append tests)

**Interfaces:**
- Consumes: `testRender` from `@opentui/solid`, the installed `globalThis.__arabcodeRtl.editor` hooks, `visualLine` from `rtl.ts`. The textarea renderable is grabbed via a Solid `ref`.

- [ ] **Step 1: Write the failing tests**

Append to `packages/tui/test/util/rtl-render.test.tsx`:

```tsx
test("typed Arabic renders shaped, reordered, right-aligned; cursor maps visually", async () => {
  let ta: any
  const app = await testRender(() => <textarea ref={(r: any) => (ta = r)} width={20} height={3} />)
  try {
    ta.insertText("سلام")
    await app.renderOnce()
    const frame = app.captureCharFrame()
    const row = frame.split("\n")[0]!
    // مشكّل ومعاد ترتيبه (م المنعزلة أولاً بصرياً) ومحاذى يميناً لعرض 20
    expect(row.trimEnd()).toBe(" ".repeat(17) + visualLine("سلام"))
    // المؤشر المنطقي بعد 4 أعمدة ← بصرياً على الخلية يسار "م" (عرض بصري 3، حشوة 17)
    const col = globalThis.__arabcodeRtl!.editor.cursorCol(ta, ta.editorView.getVisualCursor())
    expect(col).toBe(16)
  } finally {
    app.renderer.destroy()
  }
})

test("arrow keys move visually in RTL context", async () => {
  let ta: any
  const app = await testRender(() => <textarea ref={(r: any) => (ta = r)} width={20} height={3} />)
  try {
    ta.insertText("سلام")
    await app.renderOnce()
    expect(ta.logicalCursor.col).toBe(4)
    expect(globalThis.__arabcodeRtl!.editor.swapArrows(ta)).toBe(true)
    // سهم يمين بصري = رجوع منطقي (المعالج المرقوع يقلب)
    ta._actionHandlers.get("move-right")()
    expect(ta.logicalCursor.col).toBe(3)
    ta._actionHandlers.get("move-left")()
    expect(ta.logicalCursor.col).toBe(4)
  } finally {
    app.renderer.destroy()
  }
})

test("LTR textarea content takes the native path untouched", async () => {
  let ta: any
  const app = await testRender(() => <textarea ref={(r: any) => (ta = r)} width={20} height={3} />)
  try {
    ta.insertText("hello")
    await app.renderOnce()
    expect(app.captureCharFrame()).toContain("hello")
    expect(globalThis.__arabcodeRtl!.editor.cursorCol(ta, ta.editorView.getVisualCursor())).toBeUndefined()
    expect(globalThis.__arabcodeRtl!.editor.swapArrows(ta)).toBe(false)
  } finally {
    app.renderer.destroy()
  }
})

test("backspace removes the last typed Arabic letter (logical) and frame updates", async () => {
  let ta: any
  const app = await testRender(() => <textarea ref={(r: any) => (ta = r)} width={20} height={3} />)
  try {
    ta.insertText("سلام")
    await app.renderOnce()
    ta.deleteCharBackward()
    await app.renderOnce()
    expect(app.captureCharFrame().split("\n")[0]!.trimEnd()).toBe(" ".repeat(18) + visualLine("سلا"))
  } finally {
    app.renderer.destroy()
  }
})
```

Notes for the implementer:
- If `ref` is not supported by `@opentui/solid`'s `<textarea>`, walk the renderable tree instead: `const ta = [...app.renderer.root.getChildren()].find((c: any) => c.constructor.name === "TextareaRenderable")` (recurse if nested). Check `getChildren()` exists on `Renderable`; adapt to the actual traversal API found in `@opentui/core`.
- The exact expected cursor value (16) follows from: raw width 4 → logical cursor col 4; shaped visual width 3 (lam-alef merges) → pad 17; `cursor[4] = visualStart(م) - 1 + pad = 0 - 1 + 17 = 16`.
- `visualLine("سلا")` after backspace: text is "سلا" (deleted م), shaped width 2 (س + لا ligature) → pad 18.
- If the first row assertion fails on exact padding, print the frame — off-by-one here means Zig's width method disagrees with `Bun.stringWidth` for a char; investigate before loosening the assertion (Global Constraint: logical raw cols must match Zig's counting).

- [ ] **Step 2: Run tests to verify current state**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/rtl-render.test.tsx`
Expected: the 4 new tests PASS if Tasks 1–3 are correct — this is the empirical validation of the `visualCol`-semantics assumption (that `visualCursor.visualCol` equals the logical column within the virtual line). If `cursorCol` returns wrong values, debug in this order: (1) log `ta.editorView.getVisualCursor()` after each `insertText` — if `visualCol` is NOT the raw logical column within the line, adjust the indexing in `rtl-editor.ts` `cursorCol` accordingly and document the actual semantics in a comment; (2) log `ta.editorView.getLineInfo()` to confirm `lineWidthCols`/`lineSources` semantics against `sliceVirtualLines` assumptions and fix the slicer if Zig reports widths that include eaten wrap spaces.

- [ ] **Step 3: Full suite regression check**

Run: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/`
Expected: all rtl* files PASS; no new failures vs. the branch baseline elsewhere.

- [ ] **Step 4: Commit**

```bash
git add packages/tui/test/util/rtl-render.test.tsx
git commit -m "test(tui): integration tests for RTL input mirror (frame, cursor map, visual arrows)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: Real-app verification with screenshots + memory update

**Files:**
- Modify: `/home/abotrf/.claude/projects/-home-abotrf-Desktop-opencode/memory/arabcode-rtl-display.md` (update phase-2 status)

**Interfaces:**
- Consumes: everything above; the screenshot workflow from phase 1 (gnome-terminal + xdg portal DBus — `gnome-screenshot`/`grim` do NOT work on this GNOME Wayland setup).

- [ ] **Step 1: Launch the real app**

```bash
gnome-terminal -- bash -c "cd /home/abotrf/Desktop/opencode && timeout 20 bun dev"
```

Wait ~4s for startup, then type Arabic into the prompt programmatically is NOT possible — instead use `xdotool type --delay 120 'سلام عليكم'` targeting the gnome-terminal window (`xdotool search --name` may not work on Wayland; if unavailable, fall back to asking the user to type — but FIRST try `ydotool` or verify with the test-suite evidence plus a screenshot of the app with the placeholder replaced after simulated input via the clipboard: `wl-copy 'سلام عليكم'` then simulated paste is also Wayland-restricted). Pragmatic fallback that always works: temporarily run the app with an `initialValue`-style probe — create a scratch entry `packages/tui/scratch-rtl-probe.tsx` is NOT needed; instead rely on `testRender` frame tests (Task 4) plus a manual screenshot: launch the app and take a screenshot while the USER types. If no input automation path works, report to the user: "اكتب أي جملة عربية في الموجّه وسأصوّر الشاشة" and screenshot afterwards.

- [ ] **Step 2: Screenshot via xdg portal**

Use the phase-1 python DBus snippet (SessionBus → `org.freedesktop.portal.Desktop` → `Screenshot` → listen for `Response` → copy from `file://` URI), crop to the prompt row with PIL, and visually verify: Arabic shaped + reordered + right-aligned, cursor sitting one cell left of the last typed letter.

- [ ] **Step 3: Update memory file**

In `/home/abotrf/.claude/projects/-home-abotrf-Desktop-opencode/memory/arabcode-rtl-display.md`, replace the "**Phase 2 (not done):**" bullet with a short note: phase-2 input mirror implemented (rtl-editor.ts mirror TextBuffer over native EditBuffer; cursor map via visualLineMap; visual arrows via swapArrows; selection highlight deliberately deferred), and keep the CodeRenderable/Diff exclusion note.

- [ ] **Step 4: Final verification and report**

Run once more: `cd /home/abotrf/Desktop/opencode/packages/tui && bun test test/util/ && bun run typecheck`
Expected: PASS + clean. Report to the user (in Arabic) what was built, what to try (typing, arrows, backspace, multiline), and the documented v1 limits (selection highlight, empty-line initial cursor side).
